# REQ-2026-046 리뷰 요청

## 배경

REQ-045 `03-analysis.md`가 **PROVISIONAL(1차 태깅)**을 "실증적 확증"으로 확대하고 **제외된 REQ-041**을 분류·강화 근거로 씀 → 사용자 P1 2건. 정정본은 이미 공개 `main`에 병합돼 있어 즉시 정직화한다.

## 변경 요약 (설계)

- `03-analysis.md`의 3개 절(잠정 결론·후속 후보·선택편향 한계) **문구만** 정정 — 확정 표현 하향 + 제외 REQ-041을 코호트 밖 가설로 분리.
- **32 태깅 행·집계·DEC-1 임계값·`corpus-freeze.md`·관측성 코드 불변**.

## 리뷰 포인트

- 정정이 **의미(태깅/집계/판정) 불변**이고 **주장 강도만 하향**했는가.
- 제외 REQ-041이 확정 근거에서 빠지고 **코호트 밖 외부 가설**로 분리됐는가.
- staged diff의 수정 파일이 **`03-analysis.md` 하나**인가(태깅표/집계표/임계값표 라인 미접촉).
