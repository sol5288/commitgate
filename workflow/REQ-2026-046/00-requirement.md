# REQ-2026-046 요구사항 — REQ-045 분석 결론 문구 정정 (micro-REQ)

## 무엇을 / 왜

REQ-2026-045의 `workflow/REQ-2026-045/03-analysis.md`는 **PROVISIONAL(단일 태거 1차 태깅)** 임을 명시해놓고도, §잠정 결론에서 이를 "resume 질문의 **실증적 종결**"·"게이트 무-resume **확정**이 데이터로 뒷받침"으로 확대했고, §한계에서 **잠긴 코호트 밖으로 제외된 REQ-041**을 "③b가 명백"하다며 분류·강화 근거로 사용했다. 이는 문서 자신의 "2인 태깅 전 최종화 금지" 계약과 충돌하는 **over-claim**이다(사용자 P1 2건).

**왜**: 정정본은 이미 공개 `main`(`7bd263d`)에 병합돼 있어, 알려진 과장을 2인 태깅까지 남겨둘 이유가 없다. **표현만** 정직한 한정으로 바꾼다.

**경계 한 줄**: 이 REQ는 **문구 정정**이다. 태깅·집계·판정·근거 데이터를 바꾸지 않는다.

## 완료 기준 (검증 가능)

1. `workflow/REQ-2026-045/03-analysis.md`의 **§잠정 결론·§후속 REQ 후보·§한계** 문구가 정직한 한정으로 정정된다:
   - PROVISIONAL 1차 태깅 결과임을 유지하고 "실증적 종결/확증/불필요"를 **"1차 태깅에서 재론 미관측 → 재도입 트리거 미충족 → 현시점 stateless 유지"**로 한정. "resume이 어떤 상황에서도 무용하다는 반증/완전한 실증은 아님"을 명시.
   - 제외된 **REQ-041을 확정 근거에서 제거**하고 **코호트 밖 외부 맥락 가설**로 분리(편향이 결론을 강화한다고 단정하지 않음).
2. **32개 태깅 행·§집계·§DEC-1 임계값 표·§방법·`corpus-freeze.md`·관측성 코드는 변경 없음**(불변).
3. staged diff의 수정 파일이 **`03-analysis.md` 하나뿐**이다.
4. `tsc --noEmit` 0 · `req:doctor` PASS. CommitGate REQ 워크플로 준수 · **docs 단일 커밋** · B1 direct push.

## 범위 (MVP)

- `03-analysis.md`의 3개 프로즈 절(잠정 결론·후속 후보·선택편향 한계) 문구 정정만.

## 비목표 (경계)

- 32 태깅 행·§집계·§DEC-1 임계값 값 변경.
- `corpus-freeze.md`·관측성 코드(`scripts/req/review-codex.ts` 등) 변경.
- **2인 독립 태깅·adjudication·임계값 재계산** — 사람/별도 단계.
- **후속 oracle 도구위임 REQ 개설** — 2인 태깅 확정 後 별도.
- design 리뷰 증거 영속화 정책 결정 — 별도 REQ.

## 근거·불변식

- 정정은 **의미(태깅/집계/판정)를 바꾸지 않고** 결론의 **주장 강도만 낮춘다**(확정 → 잠정 한정). 근거 데이터(freeze·태깅)는 불변.
