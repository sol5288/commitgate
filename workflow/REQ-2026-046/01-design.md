# REQ-2026-046 설계 — REQ-045 분석 결론 문구 정정

> 정본: 사용자 P1 지적 2건 — ①잠정(PROVISIONAL) 결과를 "확정/실증적 종결"로 확대 금지, ②잠긴 코호트에서 제외된 표본(REQ-041)을 확정 분류·강화 근거로 사용 금지. 본 문서는 그 정정을 `03-analysis.md`에 어떻게 반영할지 기록.

## 현재 상태(변경 대상)

`workflow/REQ-2026-045/03-analysis.md`(커밋 `7bd263d`) 3개 절:
- **§잠정 결론**: "**지원됨**" 헤더 · "이는 resume 질문의 **실증적 종결**이다" · "게이트 무-resume **확정**이 데이터로 뒷받침된다" · "freeze 가설을 **정확히 확증**".
- **§후속 REQ 후보**: "Ledger·resume은 **불필요**(② 부재)".
- **§한계**: "REQ-041 … **③b가 명백**한 사례 → 소실분은 ③b를 과소표집한다(**편향이 결론을 강화**하지 반증하지 않음)".

## 핵심 설계 결정

### DEC-1 — 정정 문구(정본)
- **§잠정 결론**: 확정 표현 제거 → 정직한 결론 문장으로 교체 — "완전 아카이브 32전이 **1차 태깅**에서 재론(②) 미관측·oracle 갭(③b) 우세 → **현시점 stateless 유지**·2인 태깅 확정 시 oracle REQ 우선 검토·**resume/Ledger 게이트 재도입은 (현 트리거 기준) 지원 안 됨**". + "resume이 어떤 상황에서도 무용하다는 반증/원래 정책의 완전한 실증은 **아님**(제한적 의미: 재도입 트리거 미충족)". REQ-032(eligible)는 "일치"로 완화("확증" 제거).
- **§후속**: "불필요" → "재도입 **트리거 미충족**(② 미관측) — 확정은 2인 태깅 後".
- **§한계**: REQ-041을 확정 근거에서 제거 → "확정 결론은 **eligible 32전이·1차 태깅에만** 근거, 소실 터미널 2건은 **미관측**, 편향이 결론을 **강화한다고 단정하지 않음**". REQ-041은 **코호트 밖 외부 맥락 가설**로 분리(확정 근거 아님).

### DEC-2 — 불변 보존
32 태깅 행·§집계(③b=21·②=0 등)·§DEC-1 임계값 표·§방법·§REQ 최종결정 게이트·`corpus-freeze.md`·코드 **미변경**. 정정은 **의미 불변, 주장 강도만 하향**.

## Phase별 구현

- **phase-1-prose-correction**: 위 3개 절을 3개 정밀 편집으로 정정. 태깅 표·집계 표·임계값 표 라인은 **미접촉**.

## 변경 파일

- `workflow/REQ-2026-045/03-analysis.md` **단 하나**. (cross-ticket docs 정정 — REQ-046이 REQ-045 산출물의 문구를 고침. 사용자 지시 범위이며, 태깅 데이터가 아니라 결론 프로즈만 대상.)

## 하위호환·안전

- 프로즈만 변경 — 태깅/집계/판정/근거 데이터 불변. 결론의 **주장 강도만 하향**(over-claim 제거)이라 어떤 하위 소비자도 영향 없음.
- **단일 파일 diff**. 관측성 코드·게이트·`state`·`corpus-freeze.md` 무접촉.
